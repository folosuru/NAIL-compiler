#include "Interpreter.hpp"

namespace NAIL_cl {
    namespace interpreter {
        Interpreter::Interpreter(NodeType node) :node(node) {

        }

        void Interpreter::exec() {

        }
    } // interpreter
} // NAIL_cl